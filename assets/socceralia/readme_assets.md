Nanobanana isometric assets copied/linked for Replay2D work:

- bg_stand.png / bg_field_layer.png: cropped from `nanobanana_works/BG/usable/BG_empty_4k.png` (stand top / field bottom).
- field.png / field-lines.png / ball-idle.png / ball_shadow.png: from Socceralia package.
- isometric-nature-pack (not copied): grass1~10.png (130×130) tiles for optional field pattern.

Usage notes:
- Replay2DSimple auto-loads default paths for stand/field-layer/field/ball/shadow if export slots are empty.
- For isometric field patterns, add a TileMap in FieldLayer and tile the grass* textures; current scene does not include the TileMap.
